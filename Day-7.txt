What is an operator ?
----------------
operator is a symbol that performs a specific task.

syntax
-------
operand  operator  operand
a         +           b


MySQL Operators and Their Usage
What are Operators?
Operators are special symbols or keywords used in MySQL to perform operations on data. They help in calculations, comparing values, filtering records, combining conditions, manipulating strings, and performing bitwise operations.
Types of Operators
Arithmetic Operators
Comparison Operators
Assignment Operators
Logical Operators
String Operators
Bitwise Operators
NULL Operators
Special Operators

1. Arithmetic Operators
Definition
Arithmetic operators are used to perform mathematical calculations on numeric values.
Operator
Description
Example
Result
+
Addition
10 + 5
15
-
Subtraction
10 - 5
5
*
Multiplication
10 * 5
50
/
Division
10 / 5
2
%
Modulus (Remainder)
10 % 3
1



STEP 1: Create Table
CREATE TABLE Products (
 id INT,
 name VARCHAR(100),
 price DECIMAL(10,2),
 qty INT
 );

STEP 2: Insert Records
INSERT INTO Products VALUES
 (1, 'Pen', 10.00, 100),
 (2, 'Notebook', 25.00, 50),
 (3, 'Pencil', 5.00, 200),
 (4, 'Eraser', 3.00, 300),
 (5, 'Marker', 15.00, 80),
 (6, 'Scale', 20.00, 150),
 (7, 'Sharpener', 8.00, 120),
 (8, 'Geometry Box', 120.00, 40),
 (9, 'Sketch Pen', 60.00, 75),
 (10, 'Crayons', 90.00, 55);

SELECT Queries
Q1. Calculate the price after adding 12% GST.
SELECT name, price, price + (price * 0.12) AS price_with_tax
 FROM Products;

Q2. Calculate the discounted price after reducing ₹5.
SELECT name, price, price - 5 AS discounted_price
 FROM Products;

Q3. Find the total inventory value of each product.
SELECT name, price, qty, price * qty AS total_inventory_value
 FROM Products;

Q4. Calculate the cost of purchasing 10 units.
SELECT name, price, price * 10 AS price_for_10_units
 FROM Products;

Q5. Display products whose quantity is not divisible by 50.
SELECT *
 FROM Products
 WHERE qty % 50 <> 0;

Q6. Add ₹20 to the price of every product.
SELECT name, price, price + 20 AS increased_price
 FROM Products;

Q7. Display half of each product's quantity.
SELECT name, qty, qty / 2 AS half_quantity
 FROM Products;

Q8. Display double the quantity of every product.
SELECT name, qty, qty * 2 AS double_quantity
 FROM Products;

Q9. Find the remaining quantity after selling 10 units.
SELECT name, qty, qty - 10 AS remaining_quantity
 FROM Products;

Q10. Divide the price equally among 5 people.
SELECT name, price, price / 5 AS share_price
 FROM Products;

Q11. Display products whose price is an even number.
SELECT *
 FROM Products
 WHERE price % 2 = 0;

Q12. Display products whose quantity is divisible by 5.
SELECT *
 FROM Products
 WHERE qty % 5 = 0;

Q13. Calculate the price after giving a 10% discount.
SELECT name, price, price - (price * 0.10) AS discounted_price
 FROM Products;

Q14. Calculate the price after adding ₹15 and then 18% GST.
SELECT name, price, (price + 15) * 1.18 AS final_price
 FROM Products;

Q15. Display the remaining quantity after selling half the stock.
SELECT name, qty, qty - (qty / 2) AS remaining_stock
 FROM Products;

UPDATE Queries
Q1. Add 2 units to every product.
UPDATE Products
 SET qty = qty + 2;

Q2. Reduce the price of every product by ₹5.
UPDATE Products
 SET price = price - 5;

Q3. Double the quantity of Eraser.
UPDATE Products
 SET qty = qty * 2
 WHERE name = 'Eraser';

Q4. Reduce the quantity of Marker to half.
UPDATE Products
 SET qty = qty / 2
 WHERE name = 'Marker';

Q5. Increase the price of Notebook by ₹20.
UPDATE Products
 SET price = price + 20
 WHERE name = 'Notebook';

Q6. Increase the quantity of Pen by 50.
UPDATE Products
 SET qty = qty + 50
 WHERE name = 'Pen';

Q7. Reduce the quantity of Pencils by 25.
UPDATE Products
 SET qty = qty - 25
 WHERE name = 'Pencil';

Q8. Increase the price of Scale by 10%.
UPDATE Products
 SET price = price + (price * 0.10)
 WHERE name = 'Scale';

Q9. Double the quantity of Sharpener.
UPDATE Products
 SET qty = qty * 2
 WHERE name = 'Sharpener';

Q10. Reduce the price of Crayons by ₹15.
UPDATE Products
 SET price = price - 15
 WHERE name = 'Crayons';

DELETE Queries
Q1. Delete products whose quantity is less than 100.
DELETE FROM Products
 WHERE qty < 100;

Q2. Delete products whose price is less than ₹10.
DELETE FROM Products
 WHERE price < 10;

Q3. Delete the Marker record.
DELETE FROM Products
 WHERE name = 'Marker';

Q4. Delete products whose quantity is greater than 300.
DELETE FROM Products
 WHERE qty > 300;

Q5. Delete products whose price is greater than ₹100.
DELETE FROM Products
 WHERE price > 100;
This is plain text without any

Comparison Operators in MySQL
What are Comparison Operators?
Comparison Operators are used to compare two values or expressions. They return TRUE (1) or FALSE (0) based on whether the comparison condition is satisfied.
These operators are mainly used with the WHERE clause to filter records.

Types of Comparison Operators
Operator
Description
Example
=
Equal to
salary = 50000
!= or <>
Not Equal to
department != 'HR'
>
Greater Than
salary > 50000
<
Less Than
age < 25
>=
Greater Than or Equal To
age >= 18
<=
Less Than or Equal To
salary <= 40000

Note: In MySQL, both != and <> represent Not Equal To.


STEP 1: Create Table
CREATE TABLE StudentScores (
 id INT,
 name VARCHAR(100),
 subject VARCHAR(50),
 marks INT
 );

STEP 2: Insert Records
INSERT INTO StudentScores VALUES
 (1, 'Alice', 'Math', 95),
 (2, 'Bob', 'Math', 76),
 (3, 'Charlie', 'Math', 59),
 (4, 'Diana', 'Math', 88),
 (5, 'Eve', 'Math', 40),
 (6, 'Frank', 'Math', 95),
 (7, 'Grace', 'Science', 67),
 (8, 'Henry', 'Science', 82),
 (9, 'Ivy', 'English', 91),
 (10, 'Jack', 'English', 55),
 (11, 'Kevin', 'Math', 73),
 (12, 'Lily', 'Science', 48),
 (13, 'Mia', 'English', 100),
 (14, 'Noah', 'Math', 64),
 (15, 'Olivia', 'Science', 79);

SELECT Queries
Q1. Display students who scored more than 80.
SELECT *
 FROM StudentScores
 WHERE marks > 80;

Q2. Display students who scored 60 or more.
SELECT *
 FROM StudentScores
 WHERE marks >= 60;

Q3. Display students who scored less than 50.
SELECT *
 FROM StudentScores
 WHERE marks < 50;

Q4. Display students who scored exactly 95.
SELECT *
 FROM StudentScores
 WHERE marks = 95;

Q5. Display students who did not score 95.
SELECT *
 FROM StudentScores
 WHERE marks != 95;
